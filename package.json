{
  "name": "ares-monorepo",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "turbo run dev codegen:watch",
    "dev:web": "turbo run dev --filter @ares/web",
    "dev:api": "turbo run dev --filter @ares/api",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "format": "turbo run format || true",
    "typecheck": "turbo run typecheck",
    "prepare": "node -e \"if (process.env.CI || process.env.VERCEL) process.exit(0); const { spawnSync } = require('node:child_process'); const result = spawnSync('husky', { stdio: 'inherit', shell: true }); if (result.error) throw result.error; process.exit(result.status ?? 0);\""
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^16.4.0",
    "prettier": "^3.8.2",
    "prettier-plugin-tailwindcss": "^0.7.2",
    "turbo": "^2.9.6"
  },
  "lint-staged": {
    "apps/web/src/lib/apollo/queries/**/*.ts": [
      "pnpm -C apps/web codegen"
    ],
    "apps/web/**/*.{ts,tsx}": [
      "pnpm -C apps/web exec eslint --fix",
      "prettier --write"
    ],
    "apps/api/**/*.ts": [
      "pnpm -C apps/api exec eslint --fix",
      "prettier --write"
    ],
    "packages/core/**/*.{ts,tsx}": [
      "pnpm -C packages/core exec eslint --fix",
      "prettier --write"
    ],
    "packages/db/**/*.{ts,tsx}": [
      "pnpm -C packages/db exec eslint --fix",
      "prettier --write"
    ],
    "*.{json,md,yml,yaml}": [
      "prettier --write"
    ]
  },
  "packageManager": "pnpm@9.1.1"
}
